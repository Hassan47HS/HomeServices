@model Home_Service.ViewModel.AdminViewModel
@{
    Layout = User.IsInRole("Admin") ? "_AdminLayout" : "_Layout";
}

<h2>Approved Services</h2>

@if (Model.ApprovedServices.Any())
{
    <table class="table">
        <thead>
            <tr>
                <th>Service Name</th>
                <th>Description</th>
                <th>Name</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var service in Model.ApprovedServices)
            {
                <tr>
                    <td>@service.Title</td>
                    <td style="word-wrap:break-word; max-width:200px">@service.Description</td>
                    <td>@service.User?.Name</td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>No approved services available.</p>
}